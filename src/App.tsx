import { SearchBar } from '~/components/SearchBar'

export const App = () => {
  return (
    <main className='flex h-full min-h-screen flex-col bg-gray-900 p-6 text-gray-50'>
      <SearchBar />
    </main>
  )
}
